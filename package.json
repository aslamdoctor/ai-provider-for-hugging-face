{
  "name": "ai-provider-for-hugging-face",
  "version": "1.1.0",
  "description": "AI Provider for Hugging Face for the WordPress AI Client.",
  "private": true,
  "scripts": {
    "build": "wp-scripts build src/js/connectors.js --output-path=build",
    "start": "wp-scripts start src/js/connectors.js --output-path=build"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
